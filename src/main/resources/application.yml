spring:
    application:
      name: LibraryApi
      datasource:
        url: jdbc:postgres://localhost:5432:library
        username: postgres
        password: postgres
        driver-class-name: org.postgresql.Driver
      Jpa:
        show-sql: true
        hibernate:
          ddl-auto: update
